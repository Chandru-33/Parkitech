<section class="page-section">
  <h1>Booking confirmed</h1>

  <div class="card">
    <h2>Payment receipt</h2>
    <p><strong>Booking ID:</strong> <%= booking.id %></p>
    <p><strong>Parking space:</strong> <%= space.title %></p>
    <p><strong>Address:</strong> <%= space.address %></p>
    <p><strong>Start:</strong> <%= booking.start_time %></p>
    <p><strong>End:</strong> <%= booking.end_time %></p>
    <p><strong>Total amount:</strong> ₹<%= booking.total_price %></p>
  </div>

  <div class="card">
    <h2>Google Maps directions</h2>
    <p>
      Click the button below to open Google Maps with directions from your current location to the booked parking slot location.
    </p>
    <a href="<%= mapsUrl %>" target="_blank" class="btn primary">
      Open in Google Maps
    </a>
  </div>

  <button class="btn ghost" onclick="window.print()">Print receipt</button>
  <a href="/user/dashboard" class="btn ghost">Back to search</a>
</section>

